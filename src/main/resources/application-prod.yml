spring:
  datasource:
    url:
    username: admin
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql:

kakao:
  restApiKey:

cloud:
  aws:
    credentials:
      accessKey:
      secretKey:
