spring:
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  datasource:
    url:
    username: admin
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql:

server:
  servlet:
    session:
      cookie:
        same-site: none
        secure: true

kakao:
  restApiKey:
  redirectUrlLocalServer: http://localhost:3000/kakao/bridge
  redirectUrlDevServer: https://ganstatest.12playlist.com/kakao/bridge
  redirectUrlProdServer: https://12playlist.com/kakao/bridge

cloud:
  aws:
    credentials:
      accessKey:
      secretKey:

jwt:
  secret: