spring:
  datasource:
    url:
    username: admin
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql:

kakao:
  restApiKey:
  redirectUrlLocalServer: http://localhost:3000/kakao/bridge
  redirectUrlDevServer: https://cassette-git-develop-kimkyungmin123.vercel.app/kakao/bridge
  redirectUrlProdServer : https://cassette-git-develop-kimkyungmin123.vercel.app/bridge

cloud:
  aws:
    credentials:
      accessKey:
      secretKey:

jwt:
  secret: