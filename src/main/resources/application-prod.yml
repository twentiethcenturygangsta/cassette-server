spring:
  datasource:
    url:
    username: admin
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql:

kakao:
  restApiKey:
  redirectUrl: http://localhost:3000/kakao/bridge

cloud:
  aws:
    credentials:
      accessKey:
      secretKey:

jwt:
  secret: